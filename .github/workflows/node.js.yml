name: jest tests + ts build to js

on:
  push:
    branches: ["master", "issue_50"]
  pull_request:
    branches: ["master"]

jobs:
  jest tests:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [18.x]
    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
          cache: "npm"
      - run: npm ci
      - run: npm test

  build:
    runs-on: ubuntu-latest
    name: Builds Typescript
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Building Typescript
        uses: FuLagann/tsc-build@v1
        with:
          user-name: "Yacine Touati"
          user-email: "yacine.touati.pro@gmail.com"
          message: "build tpyescript to js GitHub Action"
